'''join(iterable): Joins the elements of an iterable (e.g., list) into a single string, separated by the string used for the method call.'''
list1 = ["hello", "world"]
print(" ".join(list1))  # "hello world"
