'''split(sep=None, maxsplit=-1): 
Splits the string at each occurrence of the separator sep 
(default is any whitespace) into a list of substrings. 
If maxsplit is specified, at most maxsplit splits are done.'''

str1 = "hello world"
print(str1.split())  # ['hello', 'world']
