Built-in Functions:
These are functions provided by Python itself, such as print(), len(), sum(), etc.
They are available for use without the need to define them.

code:
print(len("Hello"))  # Output: 5
